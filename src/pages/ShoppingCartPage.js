import { NavLink } from 'react-router-dom';

import { FooterComponent } from '../PageComponents';

function ShoppingCartPage() {
    return (
        <div className="shopping-cart-page">
            <h1>Shopping Cart</h1>

            <FooterComponent />
        </div>
    );
}

export default ShoppingCartPage;
